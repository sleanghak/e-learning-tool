import {UserDrawer} from "../../../components/templates/layouts";

export default {
    title: 'leanghak-coder/templates/layouts/UserDrawer',
    component: UserDrawer,
};

const Template = (args) => <UserDrawer {...args} />;
export const Primary = Template.bind({});
import { StudentCard } from './../../../components/organisms/cards';

export default {
    title: 'leanghak-coder/organisms/cards/StudentCard',
    component: StudentCard,
};

const Template = (args) => <StudentCard {...args} />;
export const Primary = Template.bind({});
import { Title } from '../../../components/atoms/typographys';

export default {
    title: 'leanghak-coder/atoms/typographys/Title',
    component: Title,
};

const Template = (args) => <Title {...args} />;
export const Primary = Template.bind({});
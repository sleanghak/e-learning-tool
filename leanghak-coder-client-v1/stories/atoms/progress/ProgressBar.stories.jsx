import { ProgressBar } from './../../../components/atoms/progress';

export default {
    title: 'leanghak-coder/atoms/progress/ProgressBar',
    component: ProgressBar,
};

const Template = (args) => <ProgressBar {...args} />;
export const Primary = Template.bind({});